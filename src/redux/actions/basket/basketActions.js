import axios from 'axios';
import { db } from './../../../config/firebase'

import { 
    ADD_TO_BASKET,
    REMOVE_TO_BASKET
} from './basketTypes'

export const addToBasket = () => {
    return (dispatch) => {
        
    }
}
